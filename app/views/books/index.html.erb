<p id="notice"><%= notice %></p>

<h1>Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Summanry</th>
      <th>Genre</th>
      <th>Publish date</th>
      <th>Num pages</th>
      <th>Image url</th>
      <th>Authorreference</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.summanry %></td>
        <td><%= book.genre %></td>
        <td><%= book.publish_date %></td>
        <td><%= book.num_pages %></td>
        <td><%= book.image_url %></td>
        <td><%= book.authorreference %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
